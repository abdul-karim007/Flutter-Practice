import '../widgets/textwidget.dart';
import 'package:flutter/material.dart';

contFunc(color, height, width, paramChild) {
  Container(color: color, height: height, width: width, child: paramChild);
}
